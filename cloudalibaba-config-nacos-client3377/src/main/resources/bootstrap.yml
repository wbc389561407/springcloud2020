server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #服务注册
      config:
        server-addr: 127.0.0.1:8848 #服务配置拉取
        file-extension: yaml #格式
#        group:
#        namespace:

#${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}

